import 'package:flutter/material.dart';

class WrongB extends StatelessWidget {
  const WrongB({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Un-Succesful'),
      ),
    );
  }
}
