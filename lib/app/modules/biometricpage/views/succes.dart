import 'package:flutter/material.dart';

class SuccessB extends StatelessWidget {
  const SuccessB({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Succesful'),
      ),
    );
  }
}
